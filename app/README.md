# Application Workspace

Use this directory for clone-and-own application code.

- Put product/application source files here.
- Keep governance/runtime platform code under `substrate/`.
- Keep tests near app code (for example, `app/tests/`), or integrate with existing `substrate/tests/` as needed.
