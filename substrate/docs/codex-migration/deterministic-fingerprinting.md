# Deterministic Fingerprinting Model

Module:
- `src/governed_platform/determinism/fingerprint.py`

Fingerprints:
- JSON payload fingerprint (`fingerprint_json`)
- File fingerprint (`fingerprint_file`)
- Execution fingerprint (`fingerprint_execution`)

Hash function:
- SHA-256
